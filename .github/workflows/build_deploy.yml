name: LaTeX Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install TeX Live
        run: sudo apt-get update && sudo apt-get install -y texlive-latex-recommended texlive-latex-extra texlive-fonts-recommended texlive-lang-portuguese latexmk

      - name: Build LaTeX files
        run: |
          export TEXINPUTS=".:${PWD}/latex_common//:${TEXINPUTS}:"
          find . -name "*.tex" -execdir latexmk -pdf {} \;
